package main
import "fmt"

func print_fila(fila[]int, espada int) {
    fmt.print(" ")

    for i, elemento
}

func procurar_vivo(){
    for {
        if fila[espada] = len(fila)
        if fila[espada != 0 
        return espada
        }
    }
}
func main() {
    var qtd, espada int
    fmt.Scan(&qtd, &espada)
    

    
for i := 1; i <= qtd; i++{
    fila = append(fila, i)
}

   espada -= 1
    for range qtd -1{
        print_fila := procurar_vivo(fila, espada)
        
    }

}
