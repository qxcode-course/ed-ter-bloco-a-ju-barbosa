package main
import "fmt"
func main() {
  var numero int
  fmt.Scan(&numero)

  vetor := make([]int, numero)
  vetor : male([]int, descasados) 

  for i := 0; i < numero; i++ {
    fmt.Scan(&vetor[i])
    var animal int
    fmt.Scan(&animal)
    if animal != 
  }


}
