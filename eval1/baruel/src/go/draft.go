package main
import "fmt"
func main() {
   qntd := 0
   figurinhas := make([]int, 0, qntd)
   for i := range figurinhas  {
    fmt.Scan(&figurinhas[i])
   }

   unicas := make(map[int]int, bool)
   repetidas := make([]int, 0)
   for i := range repetidas{
    fmt.Scan(&repetidas[i])
   }

   faltando := 0

   for_, unicas := figurinhas
   if !(unicas[figurinhas] {
        repetidas := append (faltando, figurinhas)
        unicas[figurinhas] == false
   } else {
    unicas[figurinhas] == true
   }
}
